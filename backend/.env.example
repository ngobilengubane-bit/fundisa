# Copy this file to .env and fill in real values. Never commit .env itself.

# Get one at https://console.anthropic.com — needed for the /api/assistant endpoint.
ANTHROPIC_API_KEY=

# Login for the admin endpoints (adding/editing/deleting bursaries, viewing subscribers).
# Change these before you deploy anywhere public.
ADMIN_USERNAME=nqobile
ADMIN_PASSWORD=changeme123

# Port the server listens on.
PORT=3001
